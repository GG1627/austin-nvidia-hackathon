import { useEffect, useState } from "react";
import { ArrowUpRight, Check, ChevronRight, Database, Play, Sparkles } from "lucide-react";
export function Home({onConnect}:{onConnect:()=>void}){return <main className="home"><header className="home-nav"><div className="brand"><span className="brand-mark">l</span><span>lore</span></div><div className="nav-note">your creative co-pilot, with receipts.</div><button className="ghost-pill">Member sign in <ChevronRight size={15}/></button></header><div className="sticker sticker-one">made for<br/>the obsessed</div><div className="sticker sticker-two">always<br/>on-ish</div><section className="home-hero"><p className="kicker"><span/> THE CREATOR OPERATING SYSTEM</p><h1>Make your<br/><i>next move</i> hit.</h1><p className="home-copy">Lore gets your creative lore, watches what is bubbling up, and turns the noise into a plan you will actually want to make.</p><div className="hero-actions"><button className="primary big" onClick={onConnect}>Connect your world <ArrowUpRight size={18}/></button><button className="watch-button"><span className="watch-play"><Play size={13} fill="currentColor"/></span> See how it thinks</button></div></section><div className="home-art"><div className="art-label l1">YOUR NOTES</div><div className="art-label l2">LIVE SIGNALS</div><div className="art-label l3">YOUR NEXT HIT</div><div className="orbit orbit-a"/><div className="orbit orbit-b"/><div className="blob blob-a"/><div className="blob blob-b"/><div className="mini-card mc-one"><span>01</span><b>your creator brain</b><small>gets more specific with every move</small></div><div className="mini-card mc-two"><span>92</span><b>tiny voice AI</b><small>the internet is circling it rn</small></div></div><footer className="home-footer"><span>NOT A CHATBOT. A WHOLE CREATIVE BRAIN.</span><span>SCROLL TO START <ArrowUpRight size={13}/></span></footer></main>}
export function ConnectWorld({onBuild,onBack}:{onBuild:()=>void;onBack:()=>void}){const [connected,setConnected]=useState<string[]>([]);const cards=[["Notion","Your ideas, rabbit holes & notes","N"],["YouTube","Your catalog and what gets replayed","YT"],["Google Calendar","Your actual creative energy","GC"],["Twitch","Your community's live obsessions","TW"]];return <main className="connect-page"><header className="connect-top"><button className="back" onClick={onBack}>Back home</button><div className="brand"><span className="brand-mark">l</span><span>lore</span></div><span className="stepper">STEP 1 OF 2</span></header><section className="connect-intro"><p className="kicker"><span/> START WITH YOUR LORE</p><h1>Show us what<br/>you are <i>about.</i></h1><p>We read the context, not just the metrics. Connect the spaces where your creative brain already lives.</p></section><section className="connect-card"><div className="card-heading"><div><span className="step">PICK YOUR PORTALS</span><h2>Bring your world in</h2></div><span className="count">{connected.length} / 4 linked</span></div><div className="integration-grid">{cards.map(([name,desc,icon])=>{const yes=connected.includes(name);return <button key={name} className={yes?"integration connected":"integration"} onClick={()=>setConnected(v=>yes?v.filter(x=>x!==name):[...v,name])}><span className="integration-icon">{icon}</span><span><b>{name}</b><small>{desc}</small></span>{yes?<Check className="check"/>:<span className="plus">+</span>}</button>})}</div><div className="connect-footer"><span>Read-only. Low drama. You are in control.</span><button className="primary" onClick={onBuild}>Build my creator brain <Sparkles size={16}/></button></div></section></main>}
export function BuildMemory({onDone}:{onDone:()=>void}){const [progress,setProgress]=useState(16);useEffect(()=>{const id=window.setInterval(()=>setProgress(v=>v>=100?100:v+2),70);return()=>window.clearInterval(id)},[]);const ready=progress>=100;return <main className="build-page"><header className="build-top"><div className="brand"><span className="brand-mark">l</span><span>lore</span></div><span>AGENT 1 / CREATOR MEMORY</span></header><section className="build-content"><div className="memory-machine"><div className="machine-ring ring-1"/><div className="machine-ring ring-2"/><div className="machine-core"><Database size={31}/></div><div className="memory-chip chip-a">notion notes</div><div className="memory-chip chip-b">video wins</div><div className="memory-chip chip-c">calendar rhythm</div><div className="memory-chip chip-d">community signals</div></div><p className="kicker"><span/> AGENT 1 IS CONNECTING DOTS</p><h1>Building your<br/><i>creative map.</i></h1><p className="build-copy">{ready?"Your creator brain is ready. It has a few opinions already.":"Finding your recurring ideas, your best formats, and the oddly specific things your audience cannot stop saving."}</p><div className="progress-wrap"><div className="progress-copy"><span>{ready?"MAP COMPLETE":"MAKING CONNECTIONS"}</span><b>{progress}%</b></div><div className="progress-track"><span style={{width:progress+"%"}}/></div></div>{ready?<button className="primary big" onClick={onDone}>Enter my command center <ArrowUpRight size={18}/></button>:<div className="build-log"><span>+ Found a pattern: benchmarks win</span><span>+ Linked 14 notes to local AI</span><span>+ Thursday is a maker day</span></div>}</section></main>}